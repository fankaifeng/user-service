server:
  port: 8082

eureka:
  client:
    service-url:
      defaultZone: http://user:password@localhost:8761/eureka/

spring:
  application:
    name: user-service
  cloud:
    config:
      profile: newdev
      discovery:
        enabled: true
        serviceId: configuration-server